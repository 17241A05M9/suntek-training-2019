//PRIME OR NOT


#include <stdio.h>
int main()
{
    int n,i,fact=0;
    scanf("%d",&n);
    for(i=2;i<n;i++)
    {
        if(n%2==0)
        {
            fact=1;
            break;
        }
    }
    if(fact==0)
      printf("PRIME");
    else
      printf("NOT PRIME");
    return 0;
}

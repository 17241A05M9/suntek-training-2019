//FACTORIAL of a no.


#include <stdio.h>
int fact(int n);
int main()
{
    int n,f;
    scanf("%d",&n);
    f=fact(n);
    printf("\nFACT:%d",f);
    return 0;
}
int fact(int n)
{
   if(n>0)
      return(n*fact(n-1));
   else
      return 1;
}
